name: Build and Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20 # Note: Node 24 is very new; 20 is the current LTS
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: octomatch
          path: dist

  dev:
    name: Deploy Dev
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: octomatch
          path: website
      - name: Deploy to Dev
        run: |
          echo "Downloaded artifact contents:"
          ls -R website # Using ls -R as 'tree' isn't always installed by default
          echo "Demo deploy step: replace with your real deploy command."